name: Linter

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  vet:
    name: Go vet
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Go 1.26
        uses: actions/setup-go@v6
        with:
          # <block name="go-version">
          go-version: '1.26'
          # </block>
      - name: Go vet
        run: go vet ./...
